export default interface userTypes {
    id: string,
    gender: string,
    age: Date,
    phone: string,
    password: string,
    device: string,
    deviceId: string
    pushToken: string
    balance: number
    name: string
    isAdmin: boolean
}